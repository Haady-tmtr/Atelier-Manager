{% extends 'base.html.twig' %}

{% block title %}Modifier l'Atelier{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h2 class="mb-3 text-primary">Modifier l'Atelier</h2>

        <div class="card shadow-sm p-4">
            {{ include('atelier/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <!-- Boutons d'action alignés -->
        <div class="d-flex justify-content-center gap-3 mt-4">
            <a href="{{ path('app_atelier_index') }}" class="btn btn-secondary">
                Retour à la liste détaillée
            </a>
            {{ include('atelier/_delete_form.html.twig', { 'class': 'd-inline' }) }}
        </div>
    </div>
{% endblock %}
